# .env のテンプレート。コピーして .env を作成し、鍵を差し替える:
#   cp .env.example .env
# 鍵生成例(PowerShell):
#   "sk-aihub-" + -join ((1..48) | % { '{0:x}' -f (Get-Random -Max 16) })
# 鍵生成例(bash):
#   echo "sk-aihub-$(openssl rand -hex 24)"
LITELLM_MASTER_KEY=sk-aihub-REPLACE_WITH_LONG_RANDOM_STRING
